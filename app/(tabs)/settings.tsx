import { Alert, ScrollView, StyleSheet, Text } from "react-native";
import { useRouter } from "expo-router";
import { AppButton, Card, StatusPill } from "@/components/erpclaw-ui";
import { ScreenContainer } from "@/components/screen-container";
import { useLocalERP } from "@/contexts/local-erp-context";
export default function SettingsScreen() { const router = useRouter(); const { ledger, reset } = useLocalERP(); return <ScreenContainer className="px-5"><ScrollView contentContainerStyle={styles.content}><Text style={styles.title}>Standalone settings</Text><Card><StatusPill label="NO SERVER REQUIRED" tone="success" /><Text style={styles.label}>Storage</Text><Text style={styles.copy}>Business data is saved only on this device. It does not synchronise automatically to another phone or a server.</Text></Card><Card><Text style={styles.label}>Local company</Text><Text style={styles.copy}>{ledger.company ? `${ledger.company.name} · ${ledger.company.currency} · ${ledger.company.country}` : "No local company has been created."}</Text></Card><AppButton title="Erase local business data" icon="delete-forever" tone="danger" onPress={() => Alert.alert("Erase local data", "This deletes all company, customer, inventory, invoice, payment, and journal data stored on this phone.", [{ text: "Cancel", style: "cancel" }, { text: "Erase", style: "destructive", onPress: () => { reset(); router.replace("/standalone-onboarding" as never); } }])} /></ScrollView></ScreenContainer>; }
const styles = StyleSheet.create({ content: { gap: 16, paddingVertical: 22 }, title: { color: "#17201E", fontSize: 28, fontWeight: "800" }, label: { color: "#17201E", fontSize: 17, fontWeight: "800" }, copy: { color: "#64716C", fontSize: 14, lineHeight: 21 } });
