import { ScrollView, StyleSheet, Text, View } from "react-native";
import { useRouter } from "expo-router";
import { Card } from "@/components/erpclaw-ui";
import { ScreenContainer } from "@/components/screen-container";
const WORKSPACES = [["Sales", "Customers, quotations, orders, delivery notes, invoices, and credit control."], ["Buying", "Suppliers, material requests, RFQs, purchase orders, receipts, and bills."], ["Inventory", "Items, warehouses, stock movements, serials, batches, valuation, and reservations."], ["Finance", "General ledger, journals, payments, tax, currencies, budgets, and financial reporting."], ["People", "Employees, leave, attendance, expense claims, payroll, W-2s, and garnishments."], ["Billing", "Usage meters, rate plans, billing periods, recurring invoicing, and prepaid credits."], ["Advanced", "Revenue recognition, leases, intercompany, eliminations, and consolidation."], ["Administration", "Companies, roles, backups, credentials, modules, migration, and audit controls."]] as const;
export default function WorkspacesScreen() { const router = useRouter(); return <ScreenContainer className="px-5"><ScrollView contentContainerStyle={styles.content}><View style={styles.header}><Text style={styles.title}>Workspaces</Text><Text style={styles.subtitle}>Choose a business domain. Each workspace routes to the corresponding actions from your connected engine.</Text></View>{WORKSPACES.map(([name, description]) => <Card key={name} style={styles.workspace}><Text accessibilityRole="button" onPress={() => router.push({ pathname: "/(tabs)/actions", params: { domain: name } } as never)} style={styles.workspaceTitle}>{name}</Text><Text style={styles.workspaceText}>{description}</Text><Text accessibilityRole="link" onPress={() => router.push({ pathname: "/(tabs)/actions", params: { domain: name } } as never)} style={styles.link}>Browse {name} actions →</Text></Card>)}</ScrollView></ScreenContainer>; }
const styles = StyleSheet.create({ content: { gap: 12, paddingVertical: 22 }, header: { gap: 8, marginBottom: 4 }, title: { color: "#17201E", fontSize: 28, fontWeight: "800" }, subtitle: { color: "#64716C", fontSize: 15, lineHeight: 21 }, workspace: { gap: 7 }, workspaceTitle: { color: "#17201E", fontSize: 18, fontWeight: "800" }, workspaceText: { color: "#64716C", fontSize: 14, lineHeight: 20 }, link: { color: "#0D5C5A", fontSize: 14, fontWeight: "800", paddingTop: 4 } });
